class HelloWorld {

    // Kotlin에서는 함수(메서드)를 fun 키워드로 정의한다.

    fun main(args: Array<String>) {

        // System.out.println() -> 표준 자바 라이브러리 함수를 간결하게 사용할 수 있게 감싼 래퍼 중 하나이다.
        println("Hello, world!")
    }
}